- defaults:
    link: 
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
